'use strict';

const db = require('./models/technology');

async function getAll (ctx) {
  try {

  } catch (error) {
    
  }
}

async function createTerm (ctx) {
  try {
    
  } catch (error) {
    
  }
}

module.exports = {
  getAll, 
  createTerm
}