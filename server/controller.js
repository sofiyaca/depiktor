'use strict';

const db = require('./models');

async function getAll (ctx) {
  try {

  } catch (error) {
    
  }
}

async function createTerm (ctx) {
  try {
    
  } catch (error) {
    
  }
}

module.exports = {
  getAll, 
  createTerm
}